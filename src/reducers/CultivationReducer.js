/*
const INITIAL_STATE = {
    cultivation: null,
};
// action:{type:"INSERT"}
export default function (state = null, action){
        switch (action.type){
            default: return state;
            case "CULTIVATION_DETAIL":
                return {
                    ...state //stato modificato
                };

 */
     /*           break;
            case "CULTIVATION_INSERT":
                return {
                    //lo stato deve essere un nuovo oggetto altrimenti non sempre si avra un
                    //cambio di stato
                    ...state //stato modificato
                };
                break;
            case "CULTIVATION_UPDATE":
                return {
                    ...state //stato modificato
                };
                break;
        }
    return state;
}

//lo stato iniziale se non è mai stato chiamato è
// {
//  app:unfedined, cultivation:undefined
//} quindi si deve inizializzare a null lo stato
//{
// app:null, cultivation:{
// cultivation:null}
// }
*/

